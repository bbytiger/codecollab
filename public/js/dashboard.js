function check_cookie() {
  // if not authenticated, return false

  // if authenticated return the username
}

function fetch_files() {
  console.log('fetching files')
  // request the files from the server with authentication key

  // for each file retrieved, create a thumbnail image and brief description (this is like a React component)
  document.getElementById('file_list').innerHTML = "this is fetching the file correctly";
}